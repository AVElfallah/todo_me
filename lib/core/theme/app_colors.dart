import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFFBDD8D1);
  static const Color buttonColor = Color(0xFF756EF3);
  static const Color secondaryTextColor = Color(0xFF868D95);
  static const Color mainTextColor = Color(0xFF002055);
  static const Color hintTextColor = Color(0xFF756EF3);
  static const Color customGreenColor = Color.fromARGB(255, 31, 189, 84);
  // in hex: 0x[CC]756EF3  cc refers to the opacity 
  // it equals 80%  because 0xCC = 204/255 = 0.8
  static const Color appBarColor = Color(0xCC756EF3); // 80% opacity

}